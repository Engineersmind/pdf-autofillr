# PDF Mapper Module — All Dependencies
# Used by: local dev, Docker image, CI
# Cloud SDKs are all included; unused ones add ~5 MB but simplify deployment.

# ── PDF Processing ────────────────────────────────────────────────────────────
PyMuPDF==1.26.5
numpy==2.3.5

# ── LLM ──────────────────────────────────────────────────────────────────────
openai>=2.8.0
litellm==1.82.0
tiktoken==0.12.0

# ── Data / Validation ─────────────────────────────────────────────────────────
pandas==2.3.3
pydantic==2.12.3
pydantic-settings==2.11.0
pydantic_core==2.41.4

# ── HTTP (clients + server) ───────────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
aiohttp==3.13.1
requests==2.32.5
fastapi==0.115.0
uvicorn[standard]==0.34.0
python-multipart>=0.0.6

# ── Cloud storage ─────────────────────────────────────────────────────────────
boto3==1.42.66                    # AWS S3
azure-storage-blob>=12.19.0       # Azure Blob
azure-identity>=1.15.0
google-cloud-storage>=2.10.0      # GCP

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.1.1
tenacity==9.1.2
Jinja2>=3.1.0
regex==2025.9.18
tqdm==4.67.1
typing-inspection==0.4.2
typing_extensions==4.15.0
python-json-logger==4.0.0
